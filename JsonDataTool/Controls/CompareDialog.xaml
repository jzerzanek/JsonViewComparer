<Window x:Class="JsonDataTool.Controls.CompareDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:jsonViewComparer="clr-namespace:JsonViewComparer;assembly=JsonViewComparer"
        mc:Ignorable="d"
        Title="Json data comparator" Height="450" Width="800">
    <Grid>
        <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
            <jsonViewComparer:JsonViewComparerControl LeftTitle="{Binding LeftTitle}" LeftValue="{Binding LeftJson.JsonValue}" RightTitle="{Binding RightTitle}" RightValue="{Binding RightJson.JsonValue}"/>
        </ScrollViewer>
    </Grid>
</Window>
